import React from 'react'
import { PageProps } from '../interfaces/PageProps'

const SignUpPage: React.FC<PageProps> = (props: PageProps) => {
  return (
    <div>
      <div>Sign Up</div>
    </div>
  )
}

export default SignUpPage
