import React from 'react'
import { PageProps } from '../interfaces/PageProps'

const SignInPage: React.FC<PageProps> = (props: PageProps) => {
  return (
    <div>
      <div>Sign In</div>
    </div>
  )
}

export default SignInPage
